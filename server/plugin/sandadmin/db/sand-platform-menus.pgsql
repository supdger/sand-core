-- Optional Sand platform menus are owned by their independently installed
-- business plugins. A zero-plugin SandAdmin installation must not register
-- routes or components that are absent from the host.
SELECT 1;
